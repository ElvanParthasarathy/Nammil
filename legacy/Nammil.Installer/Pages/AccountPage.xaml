<Page
    x:Class="Nammil_Installer.Pages.AccountPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nammil_Installer.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="WhatsApp Account Setup" Style="{ThemeResource TitleTextBlockStyle}" FontWeight="SemiBold" />

        <StackPanel Grid.Row="1" Spacing="24" Margin="0,32,0,0">
            
            <StackPanel Spacing="8">
                <TextBlock Text="Who is this account for?" FontWeight="SemiBold" />
                <TextBlock Text="This helps organize your media if you add more accounts later." Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                <TextBox x:Name="AccountNameBox" PlaceholderText="e.g., Personal, Business, Elvan" Text="Personal" CornerRadius="6" MaxWidth="300" HorizontalAlignment="Left" />
            </StackPanel>

        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12">
            <Button Content="Back" Click="Back_Click" Width="100" CornerRadius="8" />
            <Button Content="Install" Click="Install_Click" Style="{ThemeResource AccentButtonStyle}" Width="100" CornerRadius="8" />
        </StackPanel>
    </Grid>
</Page>
