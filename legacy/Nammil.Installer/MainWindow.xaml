<Window
    x:Class="Nammil_Installer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Nammil_Installer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Nammil Setup">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base"/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Background="Transparent">
            <StackPanel Orientation="Horizontal" Margin="16,0,0,0" VerticalAlignment="Center">
                <Image x:Name="TitleBarLogo" Width="16" Height="16" Margin="0,0,8,0" />
                <TextBlock Text="Nammil Setup" VerticalAlignment="Center" FontSize="12" FontFamily="Segoe UI Variable"/>
            </StackPanel>
        </Grid>

        <!-- Navigation Frame -->
        <Frame x:Name="RootFrame" Grid.Row="1" />
    </Grid>
</Window>
